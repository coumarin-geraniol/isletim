<source>
  @type tail_path
  path /fluentd/log/*.log
  tag apache2.*
  format apache2
</source>

<source>
  @type tail_path
  path /fluentd/log/*.log
  tag monit.*
  format none
</source>

<match **>
  @type stdout
</match>
